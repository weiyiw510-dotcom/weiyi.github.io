# 智能手表项目简历说明

## 项目描述

基于 STM32F103C8T6 设计并实现一套可编程多功能手表系统，支持 OLED 时间显示、菜单交互、按键输入、RTC 时间管理、电池电量检测、秒表和小游戏等功能。在原有裸机工程基础上完成 FreeRTOS 移植，将 UI 显示、按键扫描、周期节拍和电池采样等逻辑拆分为独立任务，并通过队列和互斥信号量实现任务间通信与共享数据保护。

## 项目职责

- 使用 Keil MDK 搭建 STM32F103 工程，配置 GPIO、ADC、RTC、OLED、按键等外设驱动接口。
- 移植 FreeRTOS 到 Cortex-M3 平台，完成 `FreeRTOSConfig.h`、内核源码、移植层和堆管理文件集成。
- 将原裸机主循环改造为多任务架构，设计 UI 任务、1 ms 周期节拍任务和电池采样任务。
- 使用 FreeRTOS 队列传递按键事件，降低 UI 逻辑与按键扫描逻辑之间的耦合。
- 使用互斥信号量保护电池电压和电量百分比等共享变量，避免多任务并发读写造成数据不一致。
- 处理 SysTick、中断入口和延时函数与 FreeRTOS 调度器之间的冲突，保证系统节拍稳定。

## 面试可讲点

- 为什么要把裸机循环改造成 RTOS 任务。
- 按键事件为什么适合用队列，而不是继续使用全局变量。
- 电池电量为什么需要独立任务，避免 UI 刷新时长时间采样 ADC。
- FreeRTOS 接管 SysTick 后，传统 `Delay_ms` 和 `Delay_us` 需要如何调整。
- STM32F103C8T6 的 RAM 较小，任务栈和堆大小为什么要保守设置。
