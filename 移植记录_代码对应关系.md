# FreeRTOS 移植记录与代码对应关系

## 从裸机到多任务的变化

原工程核心结构为：

```text
main 初始化外设
while(1)
  首页时钟
  菜单
  设置页面

TIM2_IRQHandler
  按键扫描
  秒表节拍
  小游戏节拍
```

改造后的结构为：

```text
main 初始化外设
创建 FreeRTOS 队列和互斥量
创建 UI 任务、1 ms 节拍任务、电池任务
启动 FreeRTOS 调度器
```

## 任务设计

### UI 任务

入口函数：`App_UITask`

对应文件：

```text
App\app_tasks.c
```

职责：

- 调用 `First_Page_Clock()`
- 根据返回值进入 `Menu()` 或 `SettingPage()`
- 负责 OLED 页面刷新
- 通过 `vTaskDelay()` 主动让出 CPU

### 1 ms 节拍任务

入口函数：`App_1msTickTask`

对应文件：

```text
App\app_tasks.c
```

职责：

- 使用 `vTaskDelayUntil()` 保持固定周期
- 执行 `Key3_Tick()`
- 执行 `Key_Tick()`
- 执行 `StopWatch_Tick()`
- 执行 `Dino_Tick()`

### 电池任务

入口函数：`App_BatteryTask`

对应文件：

```text
App\app_tasks.c
```

职责：

- 每 1000 ms 调用一次 `Battery_Update()`
- 将 ADC 采样从 UI 刷新流程中剥离
- 降低 UI 卡顿风险

## 队列通信

队列对象：

```c
xQueueHandle App_KeyQueue;
```

创建位置：

```text
App\app_tasks.c
```

发送位置：

```text
Hardware\Key.c
```

接收位置：

```text
Hardware\Key.c
```

说明：

按键扫描任务检测到按键释放后，把按键编号写入 `App_KeyQueue`。UI 逻辑调用 `Key_GetNum()` 时从队列中取出事件。这样按键扫描和 UI 页面逻辑之间不再依赖同一个裸全局变量。

## 互斥信号量

互斥量对象：

```c
xSemaphoreHandle App_BatteryMutex;
```

保护的数据：

```c
ADValue
VBAT
Battery_Capacity
```

更新位置：

```text
Hardware\menu.c -> Battery_Update()
```

读取位置：

```text
Hardware\menu.c -> Show_Battery()
```

说明：

电池任务负责更新电池采样数据，UI 任务负责读取并显示电池电量。两者访问同一组全局变量，因此使用互斥信号量保证读写过程不会交叉。

## SysTick 与中断适配

FreeRTOS 使用 SysTick 作为系统节拍，因此原工程中直接操作 SysTick 的延时函数需要调整。

已处理内容：

- `Delay_ms()` 在调度器启动后使用 `vTaskDelay()`。
- `Delay_us()` 改为短忙等延时，不再重新配置 SysTick。
- `SVC_Handler`、`PendSV_Handler`、`SysTick_Handler` 在 FreeRTOS 模式下由移植层提供。

相关文件：

```text
System\Delay.c
User\stm32f10x_it.c
Middlewares\FreeRTOS\portable\RVDS\ARM_CM3\port.c
```

## 工程配置变化

`Project.uvprojx` 中新增：

- 编译宏：`USE_FREERTOS`
- 头文件路径：`App`、`Middlewares\FreeRTOS\include`、`Middlewares\FreeRTOS\portable\RVDS\ARM_CM3`
- 源码分组：`App`
- 源码分组：`FreeRTOS`

## 后续可扩展方向

- 增加任务运行状态页面，显示任务栈剩余量。
- 将 OLED 访问也封装为显示任务，其他任务通过消息队列提交显示命令。
- 将 MPU6050 姿态采样独立为传感器任务。
- 增加低功耗任务，根据长时间无操作进入省电模式。
- 增加串口调试输出，用于观察任务调度和按键事件。
